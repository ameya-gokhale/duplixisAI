{
<<<<<<< HEAD
  "name": "duplixisai",
  "type": "module",
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0",
    "npm": "please use pnpm"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "check": "pnpm -r --if-present run check",
    "fix": "pnpm -r --if-present run fix"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
=======
  "name": "linguadedup",
  "version": "1.0.0",
  "description": "Multilingual CSV deduplication",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run start --prefix client\"",
    "sample": "node generate-sample.js > sample.csv && echo 'sample.csv created'"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
>>>>>>> c565fd9 (working prototye_high_time_complexity)
  }
}
